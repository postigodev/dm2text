# Reference Website Design

Replicate `references/index.html` and `references/privacy.html` in the Astro website. Preserve the reference layout, typography, spacing, responsive behavior, and copy while reusing local brand assets and existing configuration URLs.

The home demo uses the supplied Instagram Direct screenshot with minimal anonymization: replace only the visible account identity and conversation text while preserving the real interface, geometry, colors, and controls. Store the result locally under `website/public/marketing/`.

Keep the approved privacy-policy contract, static Astro output, canonical metadata, accessible navigation, focus styling, and dependency-free runtime. Update static-output tests and verify with the website build and test commands.
